import { ArgsTable, Meta, Story, Canvas } from "@storybook/addon-docs";
import Input from "./Input.vue";

<Meta
  title="Inputs/Input"
  component={Input}
  argTypes={{
    state: {
      control: {
        type: "select",
        options: [
          "",
          "success",
          "error",
        ],
      },
    },
  }}
/>

export const Template = (args, { argTypes }) => ({
  components: { Input },
  props: Object.keys(argTypes),
  template: `<Input v-bind="$props" />`,
});

export const SizesTemplate = (args, { argTypes }) => ({
  components: { Input },
  props: Object.keys(argTypes),
  template: `
    <div class="flex gap-4">
      <Input v-bind="$props" size="small" placeholder="Small input" />
      <Input v-bind="$props" size="medium" placeholder="Medium input" />
      <Input v-bind="$props" size="large" placeholder="Large input" />
    </div>
  `,
});

export const EmailTemplate = (args, { argTypes }) => ({
  components: { Input },
  props: Object.keys(argTypes),
  template: `
    <Input v-bind="$props" type="email" label="Your email" :show-icon="true" placeholder="name@example.com">
      <template slot="icon">
        <svg fill="currentColor" viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M2.003 5.884L10 9.882l7.997-3.998A2 2 0 0016 4H4a2 2 0 00-1.997 1.884z"></path><path d="M18 8.118l-8 4-8-4V14a2 2 0 002 2h12a2 2 0 002-2V8.118z"></path></svg>
      </template>
  `,
});

export const SearchTemplate = (args, { argTypes }) => ({
  components: { Input },
  props: Object.keys(argTypes),
  template: `
    <Input v-bind="$props" :show-icon="true" placeholder="Search">
      <template slot="icon">
        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
        </svg>
      </template>
    </Input>
  `,
});

export const StatesTemplate = (args, { argTypes }) => ({
  components: { Input },
  props: Object.keys(argTypes),
  template: `
    <div>
      <Input v-bind="$props" state="success" description="Well done! Some success message!" />
      <br />
      <Input v-bind="$props" state="error" description="Oh snape! There was an error." />
    </div>
  `,
});

# Input

- [Overview](#overview)
- [Props](#props)
- [Variants](#variants)

## Overview

<Canvas>
  <Story name="Overview">
    {Template.bind({})}
  </Story>
</Canvas>

## Props

<ArgsTable story="Overview" />

## Variants

### Sizes
<Canvas>
  <Story name="Sizes">
    {SizesTemplate.bind({})}
  </Story>
</Canvas>

### Email
<Canvas>
  <Story name="Email">
    {EmailTemplate.bind({})}
  </Story>
</Canvas>

### Search
<Canvas>
  <Story name="Search">
    {SearchTemplate.bind({})}
  </Story>
</Canvas>

### States
<Canvas>
  <Story name="States">
    {StatesTemplate.bind({})}
  </Story>
</Canvas>
