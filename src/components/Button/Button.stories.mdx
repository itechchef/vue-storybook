import { ArgsTable, Meta, Story, Canvas } from "@storybook/addon-docs";
import Button from "./Button.vue";

<Meta
  title="Buttons/Button"
  component={Button}
  argTypes={{ click: { action: "clicked" } }}
/>

export const Template = (args, { argTypes }) => ({
  components: { Button },
  props: Object.keys(argTypes),
  template: `<Button v-bind="$props" v-on:click="click" />`,
});

export const ColorsTemplate = (args, { argTypes }) => ({
  components: { Button },
  template: `
    <div>
      <Button className="m-1" color="primary">Primary</Button>
      <Button className="m-1" color="secondary">Secondary</Button>
      <Button className="m-1" color="white">White</Button>
      <Button className="m-1" color="dark">Dark</Button>
      <Button className="m-1" color="success">Green</Button>
      <Button className="m-1" color="danger">Red</Button>
      <Button className="m-1" color="warning">Yellow</Button>
      <Button className="m-1" color="orange2">Orange2</Button>
      <Button className="m-1" color="blue2">Blue2</Button>
      <Button className="m-1" color="magenta">Magenta</Button>
      <Button className="m-1" color="purple">Purple</Button>
    </div>
  `,
});

export const OutlinedTemplate = (args, { argTypes }) => ({
  components: { Button },
  template: `
    <div>
      <Button className="m-1" :outlined="true" color="primary">Primary</Button>
      <Button className="m-1" :outlined="true" color="secondary">Secondary</Button>
      <Button className="m-1" :outlined="true" color="white">White</Button>
      <Button className="m-1" :outlined="true" color="dark">Dark</Button>
      <Button className="m-1" :outlined="true" color="success">Green</Button>
      <Button className="m-1" :outlined="true" color="danger">Red</Button>
      <Button className="m-1" :outlined="true" color="warning">Yellow</Button>
      <Button className="m-1" :outlined="true" color="orange2">Orange2</Button>
      <Button className="m-1" :outlined="true" color="blue2">Blue2</Button>
      <Button className="m-1" :outlined="true" color="magenta">Magenta</Button>
      <Button className="m-1" :outlined="true" color="purple">Purple</Button>
    </div>
  `,
});


export const PillsTemplate = (args, { argTypes }) => ({
  components: { Button },
  template: `
    <div>
      <Button className="m-1" :pills="true" color="primary">Primary</Button>
      <Button className="m-1" :pills="true" color="secondary">Secondary</Button>
      <Button className="m-1" :pills="true" color="white">White</Button>
      <Button className="m-1" :pills="true" color="dark">Dark</Button>
      <Button className="m-1" :pills="true" color="success">Green</Button>
      <Button className="m-1" :pills="true" color="danger">Red</Button>
      <Button className="m-1" :pills="true" color="warning">Yellow</Button>
      <Button className="m-1" :pills="true" color="orange2">Orange2</Button>
      <Button className="m-1" :pills="true" color="blue2">Blue2</Button>
      <Button className="m-1" :pills="true" color="magenta">Magenta</Button>
      <Button className="m-1" :pills="true" color="purple">Purple</Button>
    </div>
  `,
});

export const SizesTemplate = (args, { argTypes }) => ({
  components: { Button },
  template: `
    <div>
      <Button className="m-1" size="extra-small">Extra Small</Button>
      <Button className="m-1" size="small">Small</Button>
      <Button className="m-1" size="medium">Medium</Button>
      <Button className="m-1" size="large">Large</Button>
      <Button className="m-1" size="extra-large">Extra Large</Button>
    </div>
  `,
});

export const DisabledTemplate = (args, { argTypes }) => ({
  components: { Button },
  template: `
    <div>
      <Button className="m-1" :disabled="true" color="primary">Primary</Button>
      <Button className="m-1" :disabled="true" color="secondary">Secondary</Button>
      <Button className="m-1" :disabled="true" color="white">White</Button>
      <Button className="m-1" :disabled="true" color="dark">Dark</Button>
      <Button className="m-1" :disabled="true" color="success">Green</Button>
      <Button className="m-1" :disabled="true" color="danger">Red</Button>
      <Button className="m-1" :disabled="true" color="warning">Yellow</Button>
      <Button className="m-1" :disabled="true" color="orange2">Orange2</Button>
      <Button className="m-1" :disabled="true" color="blue2">Blue2</Button>
      <Button className="m-1" :disabled="true" color="magenta">Magenta</Button>
      <Button className="m-1" :disabled="true" color="purple">Purple</Button>
    </div>
  `,
});

# Button

- [Overview](#overview)
- [Props](#props)
- [Usage](#usage)
- [Variants](#variants)

## Overview

Buttons allow users take actions with a single click. They should be clear indications that they triggering an action, buttons should be easy to find among other UI.

<Canvas>
  <Story name="Overview" args={{ label: "Button" }}>
    {Template.bind({})}
  </Story>
</Canvas>

## Props

<ArgsTable story="Overview" />

## Usage
- Buttons may contain icon, on the left or the right side
- Use 8 px spacing between buttons
- Replace text with a loader if action is submitted, but still processing",
- Button width is set by it’s content, avoid changing it width",
- Use only one primary button, and any remaining calls to action should be represented as lower emphasis buttons"

## Variants

### Colors
<Canvas>
  <Story name="Colors">
    {ColorsTemplate.bind({})}
  </Story>
</Canvas>

### Outlined
<Canvas>
  <Story name="Outlined">
    {OutlinedTemplate.bind({})}
  </Story>
</Canvas>

### Pills
<Canvas>
  <Story name="Pills">
    {PillsTemplate.bind({})}
  </Story>
</Canvas>

### Sizes
<Canvas>
  <Story name="Sizes">
    {SizesTemplate.bind({})}
  </Story>
</Canvas>

### Disabled
<Canvas>
  <Story name="Disabled">
    {DisabledTemplate.bind({})}
  </Story>
</Canvas>
